# Injected automatically on Deno Deploy and by `deno run --tunnel`.
# Copy a connection string from the Deno Deploy dashboard (Databases -> URL)
# to run against a hosted timeline from your machine.
DATABASE_URL="postgresql://user:password@host:5432/database"

# Required by POST /api/wedding/reset, which deletes every invite.
WEDDING_ADMIN_TOKEN=""
